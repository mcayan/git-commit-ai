# OpenAI API Key（必填）
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI API 地址（可选，默认为 OpenAI 官方地址）
# 如果使用第三方兼容服务（如 DeepSeek），可以修改此项
# OPENAI_BASE_URL=https://api.deepseek.com/v1

# 使用的模型名称（可选，默认为 gpt-4o-mini）
# OPENAI_MODEL=gpt-4o-mini
